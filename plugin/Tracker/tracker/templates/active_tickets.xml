<?xml version="1.0" encoding="UTF-8"?>

<tickets xmlns:py="http://genshi.edgewall.org/">
    <py:for each="ticket in tickets">
        <ticket id="${ticket.id}" summary="${ticket.summary}" />
    </py:for>
</tickets>